[project]
name = "guitar-note-detector"
version = "0.1.0"
description = "Live guitar note detection from microphone audio"
requires-python = ">=3.8"
dependencies = [
    "numpy>=1.24.0",
    "pyaudio>=0.2.13",
]
